Example Todo App in Backbone
===

Getting Started
---

```sh
$ cd backbone_example
$ rake db:create && rake db:migrate && rake db:seed && rake db:test:prepare
$ open http://localhost:3000 && rails s
```
